User is authenticated but not authorized. even user is added in data admin console and given access, i added app secret and link for admin console where user is added and access is provided, when it gives me step to make changes after adding in Step 2 — Find the right file in that project Search the project for any of these patterns — that's where the authorization call goes: grep -r "authorized|access|permission|entraObjectId|microsoft|msal" --include=".ts" --include=".js" src/ Look for one of these files (common patterns in Replit apps): What you see File to edit Express/Node backend server/auth.ts, server/middleware.ts, or server/routes.ts React app calling an API client/src/hooks/useAuth.ts or src/lib/auth.ts A single-file server server/index.ts or index.js Environment config .env file at the project root

Step 3 — Add this authorization check Once you find where users are validated after login, add this call. The oid claim comes from the Microsoft token your app already receives: If the backend is Node/Express: // After Microsoft SSO login — put this in your auth middleware async function checkAdminConsoleAccess(entraObjectId: string): Promise { const res = await fetch( ${process.env.ADMIN_CONSOLE_URL}/api/access-check?entraObjectId=${entraObjectId}&app=Field Service Calendar, { headers: { "X-API-Key": process.env.ADMIN_CONSOLE_API_KEY! } } ); const data = await res.json(); return data.allowed === true; } If it uses MSAL (Microsoft Authentication Library) on the frontend: // After acquireTokenSilent(), extract the object ID from the account const entraObjectId = account.idTokenClaims?.oid; // ← this is the GUID const res = await fetch( ${import.meta.env.VITE_ADMIN_CONSOLE_URL}/api/access-check + ?entraObjectId=${entraObjectId}&app=Field Service Calendar, { headers: { "X-API-Key": import.meta.env.VITE_ADMIN_CONSOLE_API_KEY } } ); const { allowed, reason, permissions } = await res.json(); if (!allowed) { // Show the user the reason — e.g. "User has no permissions for app..." throw new Error(reason); } // Store permissions to show/hide features